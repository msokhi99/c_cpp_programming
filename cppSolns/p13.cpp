#include<iostream>

int main(){
    char key, choice;
    int num, count;
    long date;
    float yield;
    double price;

    std::cout<<"Memory address of the variable 'key': "<<&key<<"\n";
    std::cout<<"Memory address of the variable 'choice': "<<&choice<<"\n";
    std::cout<<"Memory address of the variable 'num': "<<&num<<"\n";
    std::cout<<"Memory address of the variable 'count': "<<&count<<"\n";
    std::cout<<"Memory address of the variable 'date': "<<&date<<"\n";
    std::cout<<"Memory address of the variable 'yield': "<<&yield<<"\n";
    std::cout<<"Memory address of the variable 'price': "<<&price<<"\n";
    
    return 0;
}